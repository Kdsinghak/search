import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    splashimg: {
      width: '100%',
      height: '100%',
      resizeMode: 'cover',
    },
  });
  
  export default styles