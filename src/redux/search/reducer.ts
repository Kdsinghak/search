const initialSate = {
  data: [],
};

const searchData = (state = initialSate, action: any) => {
  const {type, payload} = action;
  switch (type) {
    // case value:

    //     break;

    default:
      break;
  }
};

export default searchData;
